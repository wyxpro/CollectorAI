# 📚 Collector AI-智能阅读助手

<div align="center">

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![React](https://img.shields.io/badge/React-19.2.3-61dafb.svg)
![TypeScript](https://img.shields.io/badge/TypeScript-5.8.2-3178c6.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

**一个基于AI的游戏化阅读平台，让阅读变得更智能、更有趣**

[在线演示](https://readai.app) · [功能特性](#-核心功能) · [快速开始](#-快速开始) · [文档](#-文档)

</div>

---

## 📋 项目简介

Collector + 是一个创新的智能阅读平台，通过AI技术和游戏化机制，将传统阅读转变为互动式学习体验。平台集成了文章收录、AI问答、知识卡片、播客生成等多种功能，帮助用户更高效地获取和巩固知识。

### ✨ 核心亮点

- 🤖 **AI驱动**：基于Google Gemini AI，提供智能问答、内容解析和个性化推荐
- 🎮 **游戏化学习**：通过挑战、连胜、等级系统激励用户持续学习
- 📱 **全平台适配**：完美支持桌面、平板和移动设备
- 🎨 **精美UI**：现代化设计，流畅动画，优秀的用户体验
- 🔄 **实时同步**：数据实时保存，跨设备无缝切换
- 🌙 **深色模式**：支持浅色/深色/跟随系统三种主题

---

## 🛠️ 技术栈

### 前端框架
- **React 19.2.3** - 最新的React框架，支持并发特性
- **TypeScript 5.8.2** - 类型安全的JavaScript超集
- **Vite 6.2.0** - 极速的前端构建工具

### UI & 样式
- **Tailwind CSS** - 实用优先的CSS框架
- **Lucide React** - 精美的图标库
- **自定义动画** - 流畅的过渡和交互效果

### AI & 数据
- **Google Gemini AI** - 强大的AI能力支持
- **本地Mock数据** - 开发环境数据模拟
- **RESTful API** - 标准化的API接口设计

### 开发工具
- **ESLint** - 代码质量检查
- **Prettier** - 代码格式化
- **Git** - 版本控制

---

## 🏗️ 项目架构

```
Collector + 采用模块化架构设计，分层清晰，易于维护和扩展

┌─────────────────────────────────────────────────────────┐
│                     用户界面层 (Views)                    │
│  Dashboard │ Library │ Quiz │ Podcast │ Knowledge │ ...  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   业务逻辑层 (Hooks)                      │
│  useLibrary │ useQuiz │ usePodcast │ useUser │ ...      │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   API客户端层 (Client)                    │
│  libraryClient │ quizClient │ podcastClient │ ...        │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   API接口层 (API)                         │
│  libraryApi │ quizApi │ podcastApi │ userApi │ ...      │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   数据层 (Data/Backend)                   │
│  Mock Data │ Local Storage │ Backend API │ Database     │
└─────────────────────────────────────────────────────────┘
```

### 架构特点

1. **分层架构**：清晰的职责分离，便于维护和测试
2. **组件化设计**：可复用的UI组件和业务逻辑
3. **类型安全**：完整的TypeScript类型定义
4. **响应式设计**：移动端优先的响应式布局
5. **性能优化**：懒加载、代码分割、缓存策略

---

## 📁 目录结构

```
Read-AI---Gamified-Reading-/
├── 📂 api/                      # API接口层
│   ├── libraryApi.ts           # 文章收录API
│   ├── libraryClient.ts        # 文章收录客户端
│   ├── libraryHooks.ts         # 文章收录Hooks
│   ├── quizApi.ts              # 问答系统API
│   ├── quizClient.ts           # 问答系统客户端
│   ├── quizHooks.ts            # 问答系统Hooks
│   ├── podcastApi.ts           # 播客系统API
│   ├── podcastClient.ts        # 播客系统客户端
│   ├── podcastHooks.ts         # 播客系统Hooks
│   ├── userApi.ts              # 用户系统API
│   ├── userClient.ts           # 用户系统客户端
│   └── userHooks.ts            # 用户系统Hooks
│
├── 📂 views/                    # 页面视图层
│   ├── Dashboard.tsx           # 首页仪表盘
│   ├── LibraryView.tsx         # 文章收录夹
│   ├── AIQuizView.tsx          # AI问答挑战
│   ├── PodcastView.tsx         # AI播客生成
│   ├── KnowledgeBase.tsx       # 知识卡片库
│   ├── ReaderView.tsx          # 阅读器视图
│   ├── SettingsView.tsx        # 个人中心
│   ├── LearningData.tsx        # 学习数据
│   └── SubscriptionView.tsx    # 订阅管理
│
├── 📂 components/               # 可复用组件
│   └── QuizCard.tsx            # 问答卡片组件
│
├── 📂 data/                     # 数据层
│   ├── mockData.ts             # 模拟数据
│   └── mockUserData.ts         # 用户模拟数据
│
├── 📂 dist/                     # 构建输出目录
├── 📂 node_modules/             # 依赖包
│
├── 📄 App.tsx                   # 应用主组件
├── 📄 index.tsx                 # 应用入口
├── 📄 index.html                # HTML模板
├── 📄 types.ts                  # TypeScript类型定义
├── 📄 vite.config.ts            # Vite配置
├── 📄 tsconfig.json             # TypeScript配置
├── 📄 tailwind.config.js        # Tailwind配置
├── 📄 package.json              # 项目配置
└── 📄 README.md                 # 项目文档
```

---

## ⚡ 核心功能模块

### 1. 📚 文章收录夹 (Library)

**功能描述**：智能文章管理系统，支持多平台内容收录和组织

**核心特性**：
- ✅ 文章CRUD操作（创建、读取、更新、删除）
- ✅ 智能搜索和筛选（标题、作者、标签）
- ✅ 多视图切换（网格视图/列表视图）
- ✅ 阅读进度追踪
- ✅ 收藏和标签管理
- ✅ 批量操作（批量删除、批量更新状态）
- ✅ 数据导出（JSON/CSV格式）
- ✅ AI助理对话（智能推荐和问答）

**工作流程**：
```
用户添加文章 → AI解析内容 → 生成标签和摘要 → 
存储到收录夹 → 用户阅读 → 更新进度 → 生成问答题
```

**API接口**：
- `GET /api/library/articles` - 获取文章列表
- `POST /api/library/articles` - 添加新文章
- `PUT /api/library/articles/:id` - 更新文章
- `DELETE /api/library/articles/:id` - 删除文章
- `GET /api/library/statistics` - 获取统计信息

### 2. 🧠 AI问答挑战 (Quiz)

**功能描述**：基于收录内容自动生成个性化问答题，游戏化学习体验

**核心特性**：
- ✅ 每日挑战（5题混合难度）
- ✅ 多种题型（单选、多选、填空）
- ✅ 实时答题反馈
- ✅ AI智能解析
- ✅ 错题本系统
- ✅ 学习报告生成
- ✅ 连胜挑战机制
- ✅ 知识点分析

**工作流程**：
```
AI分析文章 → 生成问答题 → 用户答题 → 
实时判分 → 显示解析 → 记录错题 → 
生成学习报告 → 推荐复习内容
```

**API接口**：
- `GET /api/quiz/daily` - 获取每日挑战
- `POST /api/quiz/answer` - 提交答案
- `GET /api/quiz/mistakes` - 获取错题本
- `GET /api/quiz/report` - 获取学习报告

### 3. 🎙️ AI播客生成 (Podcast)

**功能描述**：将文章内容转换为AI语音播客，支持多种风格和语速

**核心特性**：
- ✅ 文章转播客（教育、对话、故事三种风格）
- ✅ 音频播放控制（播放、暂停、快进、快退）
- ✅ 播放速度调节（0.75x - 1.5x）
- ✅ AI声线选择（知性、活力、磁性）
- ✅ 播客脚本展示
- ✅ 播放进度保存
- ✅ 收藏和下载功能
- ✅ 播放统计

**工作流程**：
```
选择文章 → 设置风格和时长 → AI生成脚本 → 
语音合成 → 生成播客 → 用户播放 → 
记录进度 → 统计播放数据
```

**API接口**：
- `GET /api/podcast/list` - 获取播客列表
- `POST /api/podcast/generate` - 生成新播客
- `PUT /api/podcast/:id/play` - 更新播放计数
- `GET /api/podcast/settings` - 获取播放设置

### 4. 🎴 知识卡片库 (Knowledge)

**功能描述**：AI辅助的知识卡片系统，支持多种主题皮肤和3D翻转效果

**核心特性**：
- ✅ 知识卡片创建（原文+反思）
- ✅ 3D卡片翻转动画
- ✅ 多种主题皮肤（极光紫、星空流转、纸张翻页等）
- ✅ AI智能推荐主题
- ✅ 沉浸画廊模式
- ✅ 全量矩阵视图
- ✅ 分享海报生成
- ✅ 标签分类管理

**工作流程**：
```
阅读文章 → 提取关键内容 → AI生成反思 → 
创建知识卡片 → 选择主题皮肤 → 
浏览和复习 → 生成分享海报
```

### 5. 👤 个人中心 (Settings)

**功能描述**：完整的用户管理系统，支持个性化设置和数据统计

**核心特性**：
- ✅ 个人资料管理（头像、昵称、签名）
- ✅ 主题切换（浅色/深色/跟随系统）
- ✅ 通知设置（每日挑战、知识复习、社区互动）
- ✅ 阅读设置（字体、行高、阅读模式）
- ✅ 隐私设置（资料可见性、数据展示）
- ✅ 安全管理（密码修改、设备管理、双因素认证）
- ✅ 学习统计（阅读时长、存储使用、月度活跃）

**API接口**：
- `GET /api/user/profile` - 获取用户资料
- `PUT /api/user/profile` - 更新用户资料
- `GET /api/user/settings` - 获取用户设置
- `PUT /api/user/settings` - 更新用户设置
- `GET /api/user/security` - 获取安全信息

---

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0 或 yarn >= 1.22.0
- 现代浏览器（Chrome、Firefox、Safari、Edge最新版）

### 安装步骤

1. **克隆项目**
```bash
git clone https://github.com/yourusername/read-ai.git
cd read-ai
```

2. **安装依赖**
```bash
npm install
# 或
yarn install
```

3. **配置环境变量**
```bash
# 创建 .env 文件
touch .env

# 添加以下配置
GEMINI_API_KEY=your_gemini_api_key_here
```

4. **启动开发服务器**
```bash
npm run dev
# 或
yarn dev
```

5. **访问应用**
```
打开浏览器访问: http://localhost:3000
```

### 构建生产版本

```bash
# 构建
npm run build
# 或
yarn build

# 预览构建结果
npm run preview
# 或
yarn preview
```

---

## ⚙️ 部署指南

### Vercel 部署（推荐）

1. **安装 Vercel CLI**
```bash
npm install -g vercel
```

2. **登录 Vercel**
```bash
vercel login
```

3. **部署项目**
```bash
vercel
```

4. **配置环境变量**
- 在 Vercel 控制台添加 `GEMINI_API_KEY`

### Netlify 部署

1. **安装 Netlify CLI**
```bash
npm install -g netlify-cli
```

2. **构建项目**
```bash
npm run build
```

3. **部署**
```bash
netlify deploy --prod --dir=dist
```

### Docker 部署

1. **创建 Dockerfile**
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "run", "preview"]
```

2. **构建镜像**
```bash
docker build -t read-ai .
```

3. **运行容器**
```bash
docker run -p 3000:3000 -e GEMINI_API_KEY=your_key read-ai
```

### 服务器部署

1. **构建项目**
```bash
npm run build
```

2. **配置 Nginx**
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /path/to/read-ai/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

3. **启动服务**
```bash
sudo systemctl restart nginx
```

---

## 📦 API 接口文档

### 文章收录 API

#### 获取文章列表
```typescript
GET /api/library/articles
Response: LibraryArticle[]
```

#### 添加文章
```typescript
POST /api/library/articles
Body: {
  title: string;
  url: string;
  source: string;
}
Response: { success: boolean; article: LibraryArticle }
```

#### 更新文章
```typescript
PUT /api/library/articles/:id
Body: Partial<LibraryArticle>
Response: { success: boolean; article: LibraryArticle }
```

#### 删除文章
```typescript
DELETE /api/library/articles/:id
Response: { success: boolean; message: string }
```

### 问答系统 API

#### 获取每日挑战
```typescript
GET /api/quiz/daily?count=5&difficulty=mixed
Response: QuizQuestion[]
```

#### 提交答案
```typescript
POST /api/quiz/answer
Body: {
  questionId: string;
  answer: number | number[];
  timeSpent: number;
}
Response: QuizResult
```

#### 获取错题本
```typescript
GET /api/quiz/mistakes
Response: QuizQuestion[]
```

### 播客系统 API

#### 生成播客
```typescript
POST /api/podcast/generate
Body: {
  content: string;
  style: 'educational' | 'conversational' | 'storytelling';
  duration: 'short' | 'medium' | 'long';
}
Response: { success: boolean; podcast: Podcast }
```

#### 更新播放设置
```typescript
PUT /api/podcast/settings
Body: {
  speed?: number;
  voice?: string;
}
Response: { success: boolean; settings: PodcastSettings }
```

### 用户系统 API

#### 获取用户资料
```typescript
GET /api/user/profile
Response: UserProfile
```

#### 更新用户设置
```typescript
PUT /api/user/settings
Body: UpdateSettingsRequest
Response: { success: boolean; settings: UserSettings }
```

---

## 💡 常见问题

### Q1: 如何获取 Gemini API Key？

**A:** 
1. 访问 [Google AI Studio](https://makersuite.google.com/app/apikey)
2. 登录 Google 账号
3. 创建新的 API Key
4. 将 API Key 添加到 `.env` 文件

### Q2: 为什么播客无法播放？

**A:** 可能的原因和解决方案：
- **浏览器限制**：某些浏览器阻止自动播放，需要用户交互
- **网络问题**：检查网络连接和音频URL是否可访问
- **CORS问题**：确保音频资源配置了正确的CORS头
- **格式不支持**：确认浏览器支持音频格式（MP3/OGG/WAV）

### Q3: 移动端显示不正常怎么办？

**A:** 
1. 清除浏览器缓存
2. 确保使用最新版本的浏览器
3. 检查viewport设置是否正确
4. 查看控制台是否有错误信息
